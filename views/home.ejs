<%- include("partials/header"); -%>
<h1>Home</h1>

  <% posts.forEach(function(post){ %>
    <h1><%= (post.title) %></h1>
    <p><%= post.blog.substring(0, 50) + " ..." %>
    <a href="/posts/<%= post.title %>"> Read more</a>
  </p>

 <%  }) %>

<%- include('partials/footer'); -%>
